# Agent Guidelines

- Prefer `const x = ...` over `function x(...)` wherever possible.
- Use minimal CSS classes and node-specific attributes. Prefer consistent CSS styling across the whole site.
- Where possible, reuse functions and components to reduce duplication.
- For any UI change, include at least one updated screenshot on the resulting PR so that the reviewer can visually verify correctness.
